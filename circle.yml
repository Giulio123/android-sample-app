dependencies:
  override:
    - echo y | android update sdk --no-ui --all --filter tools,platform-tools,build-tools-23.0.1,android-23
    - ANDROID_HOME=/usr/local/android-sdk-linux ./gradlew dependencies

test:
  override:
    - ./gradlew clean test

general:
  artifacts:
    - "app/build/outputs/apk"

deployment:
  production:
    branch: master
    commands:
      - cp app/src/main/java/com/multunus/cdapp/Configuration.java.example app/src/main/java/com/multunus/cdapp/Configuration.java
      - ./gradlew build --stacktrace
